# Hardhat build outputs (regenerated by `pnpm build`)
artifacts/
cache/

# Deployment credentials — never commit (see .env.example)
.env

# TypeChain bindings (regenerated — see issue 02)
typechain-types/

# Compiled TS entry (regenerated — see issue 03)
dist/
lib.commonjs/
lib.esm/

# Test coverage
coverage/
