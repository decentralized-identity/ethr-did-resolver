---
'ethr-did-registry': major
---

The registry now lives in a new repository, along with ethr-did-resolver. The deployments export is dropped. The canonical deployments table remains part of the resolver package.
